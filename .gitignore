__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
.env.local
.DS_Store
*.egg-info/
.pytest_cache/

# Smoke test artifacts are regenerated on every run. Real Phase 4 runs
# are force-added explicitly so the submission includes them.
runs/*smoke*
runs/*.tmp

# Local-only Claude Code skills installed via third-party tooling
# (e.g. `npx uipro-cli init --ai claude`). Not part of the submission.
.claude/
