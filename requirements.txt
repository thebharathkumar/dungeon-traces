anthropic>=0.39.0
python-dotenv>=1.0.0
# tenacity drives the retry/backoff wrapper around client.messages.create.
# Required at runtime so a flaky network does not lose a Phase 4 run.
tenacity>=8.2.3
# Langfuse is optional at runtime: the LangfuseSink is a no-op when the
# package is missing or LANGFUSE_PUBLIC_KEY / LANGFUSE_SECRET_KEY are unset.
# Pinned loosely because the SDK evolves quickly.
langfuse>=2.0.0
